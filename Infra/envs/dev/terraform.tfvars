# Arquivo de variáveis para o ambiente `dev`.
# AVISO: os valores abaixo (nome do bucket e região) são literais referentes a recursos existentes.
# Alterar o bucket pode causar perda de acesso ao state — tenha certeza antes de modificar.
bucket_name = "pedromendes-dev-us-east-2-tarraform-statefile"
aws_region = "us-east-2"
